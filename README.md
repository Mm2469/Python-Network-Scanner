## Description
This is a custom ARP Network Scanner built with Python and Scapy. It scans a target IP range and returns the IP and MAC addresses of all connected devices.

## Requirements
* Python 3
* Scapy (`pip install scapy`)

## How to Run
Run the script with root privileges:
```bash
sudo python3 scanner.py
```
